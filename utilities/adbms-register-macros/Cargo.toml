# Crate for helper proc macros shared by the ADBMS-family drivers.

[package]
name = "adbms-register-macros"
edition = "2024"
publish = false
[lib]
proc-macro = true
[dependencies]
syn = { version = "3", features = ["full"] }
quote = "1"
proc-macro2 = "1"

[lints]
workspace = true
